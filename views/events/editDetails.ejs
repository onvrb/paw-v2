<!DOCTYPE html>
<html>
  <head>
    <title>Event Detail</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <h1>Event Detail</h1>
      <form action="/events/edit/<%=event._id%>" method="post">
        <table>
          <tbody>
          <tr>
              <td>Id</td>
              <td>:</td>
              <!-- Como o id não pode ser alterado usamos o input hidden para o enviar como campo do formulário. -->
              <td><input type="hidden" name="_id" id="_id" value="<%= event._id %>"><%= event._id %></td>
              </tr>
          <tr>
              <td>Name</td>
              <td>:</td>
              <td><input type="text" name="name" id="name" value="<%= event.name %>"></td>
            </tr>
            <tr>
              <td>Description</td>
              <td>:</td>
              <td><input type="text" name="description" id="description" value="<%= event.description %>"></td>
            </tr>
            <tr>
              <td>Poster image (URL)</td>
              <td>:</td>
              <td><input type="text" name="poster" id="poster" value="<%= event.poster %>"></td>
            </tr>
            <tr>
              <td>Number of tickets available</td>
              <td>:</td>
              <td><input type="number" name="nTicketsAvailable" id="nTicketsAvailable" value="<%= event.nTicketsAvailable %>"></td>
            </tr>
          <tr>
            <td>Number of tickets purchased</td>
            <td>:</td>
            <!-- Numero de bilhetes comprados não podem ser mudados -->
            <td><input type="hidden" name="event.nTicketsPurchased" id="event.nTicketsPurchased" value="<%= event.nTicketsPurchased %>">
              <%= event.nTicketsPurchased %>
            </td>
          </tr>
          <tr>
            <tr>
              <td>Price</td>
              <td>:</td>
              <td><input type="number" name="price" id="price" value="<%= event.price %>"></td>
            </tr>
            <tr>
              <td colspan="2"><button type="submit">Save edit</button></td>
            </tr>
          </tbody>
        </table>
    </form>
    </div>
    <h3><a href="/events">Ver todos os event</a></h3>
  </body>
</html>

